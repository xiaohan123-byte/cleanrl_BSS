# cleanrl_BSS 项目梳理

> 本文档用于快速掌握本仓库的结构与研究主线。梳理时间：2026-08-31（基于最新 git 提交 `4f677ab` 与工作区状态）。

## 一句话概括

本仓库 = **CleanRL 官方库（v2.0.0b1）原版底座** + **你自己的研究项目 `ExpresswayBSSs/`**。

研究主题是**高速公路换电站（Battery Swap Station, BSS）网络的 MPC–RL 分层协同运营优化**，对应论文《考虑异质用户的高速公路换电站运营 MPC-RL 分层协同优化》（`ExpresswayBSSs/paper/main.tex`）。

核心方法论：

- **MPC（滚动时域 Gurobi MILP）**：负责预约用户的剩余换电路径分配与站内服务事件调度；
- **RL（PPO，尚未接入）**：规划负责站内逐槽充电功率；critic 的终端 SOC 边际价值 λ^S = ∂V/∂S 作为长期价值项反馈给 MPC 目标函数。

---

## 一、顶层结构速览

| 路径 | 性质 | 说明 |
|---|---|---|
| `cleanrl/` | CleanRL 原版 | 单文件 RL 算法实现（PPO/DQN/C51/SAC/TD3/DDPG 等 30+ 个），未被研究改动 |
| `cleanrl_utils/` | CleanRL 原版 | 经验回放、画图、HuggingFace 上传、Optuna 调参等工具 |
| `benchmark/` | CleanRL 原版 | 各算法基准复现的 shell/SLURM 脚本 |
| `docs/` | CleanRL 原版 | mkdocs 文档源（README、pyproject.toml 也均为原版） |
| `tests/` | CleanRL 原版 | 原版冒烟测试 |
| `cloud/` | CleanRL 原版 | Terraform / AWS 云实验配置 |
| `requirements/` | CleanRL 原版 | 按环境拆分的依赖文件 |
| **`ExpresswayBSSs/`** | **你的研究** | **当前全部活跃工作都在这里** |

CleanRL 底座中与你自己相关的痕迹只有三处：

- `cleanrl/check_gpu.py`：自写的 PyTorch/CUDA 环境检查脚本（中文输出）；
- `cleanrl/src/env_bss.py`：**早期旧版**多换电站充电调度 Gym 环境（582 行）。它依赖的 `src.instancegenerator`、`src.BSSChargingScheduling` 已迁移到 `ExpresswayBSSs/`，**目前无法独立运行，属遗留代码**；
- `cleanrl/logs/`、`cleanrl/runs/`：曾用原版 `ppo.py` 跑 CartPole 验证流程，无 BSS 训练记录。

---

## 二、ExpresswayBSSs/ —— 研究本体

### 目录结构

```
ExpresswayBSSs/
├── run_mpc.py                 # ★ 主入口（956 行），滚动 MPC 求解
├── main.py                    # PPO 超参 dataclass 草稿，未接入
├── src/                       # ★ 核心代码（8/31 最新）
│   ├── paper_mpc.py           # 按论文 main.tex 实现的路径-事件联合 MILP（Gurobi）
│   ├── continuous_runner.py   # 滚动执行编排
│   ├── event_engine.py        # 确定性连续时间物理事件内核（唯一"物理真相"）
│   ├── continuous_dayahead.py # 连续日前接纳计划
│   ├── dayahead_plan.py
│   ├── mpc_model.py           # 旧逐槽 MPC（legacy）
│   ├── domain.py / time_grid.py / accounting.py / path_state.py /
│   │   reference_rollout.py / event_path_search.py   # 新架构支撑模块
│   ├── result_statistics.py   # 结果统计后处理
│   ├── cost_stress_scenarios.py # 压力测试
│   └── BSSChargingScheduling.py / instancegenerator.py / parameter.py 等  # 旧版遗留（不维护）
├── RL/
│   └── ppo.py                 # ⚠ 仍是 CleanRL 原版 CartPole 离散 PPO 骨架，未改造
├── data_generation_test/      # ★ 当前唯一在用的数据管线：带 seed 的合成 mock 数据
│   ├── parameter.py           # 默认参数（6 站×5 槽、12 时段、H=4、60kW/槽、100kWh 电池等）
│   ├── candidate_network.py   # 离线候选路径网络
│   ├── rl_data.py             # generate_mock_data + MockRLProvider / RLSignals 接口
│   └── output/                # 全部运行结果
├── data_generation_optim/     # （旧，4–5 月）真实高速路网与 OD 客流数据生成
├── data_generation_rl/        # （旧，5 月）蔚来真实数据抽取需求，未接入主流程
├── tests/                     # 15 个 unittest 文件（61 项测试通过）
├── paper/                     # ★ 论文 LaTeX（main.tex 为当前版，上交模板）
├── outputs/ logs/ runs/       # 顶层三个目录为空，留给未来 PPO 训练
└── *.md                       # 计划/笔记文档（见下）
```

### 顶层文档（很重要，是你的"工作日志"）

| 文件 | 内容 |
|---|---|
| `code_revision_plan_v3.md` | **当前代码修订执行计划**（655 行，最新基线），末尾有 2026-08-31 实施更新记录 |
| `todo.md` | MPC-RL 后续工作清单（P0–P2） |
| `exp.md` | 论文第 5 节数值实验完整设计方案 |
| `plan_mpc.md` / `plan_rl.md` | 已批准的 MPC / RL 实现计划 |
| `note.md` | 时序因果性假设：决策周期 < 首站最短行驶时间 |
| `environment.yml` | conda 环境 `py310`（python 3.10.19 + gurobipy 13.0.2） |

### 怎么跑

- 主入口：`python run_mpc.py --seed 42 --regenerate`
  （参数：`--seed/--regenerate/--network/--mock-data/--plan/--output/--time-limit/--solver-log`）
- 调用链：`run_mpc.py` → `src.continuous_runner.run_continuous_rolling_mpc` → `src.paper_mpc.solve_paper_mpc`（Gurobi 联合 MILP）→ 输出 schema-3 结果与统计文件
- 测试：`python -m unittest discover -s tests -v`
- 数据管线均可独立运行：`data_generation_test/candidate_network.py`、`rl_data.py`、`src/dayahead_plan.py`

### 数据说明

- **当前在用**：`data_generation_test/` 的 mock 数据 —— 6 站（80–580km）、2 个 O-D、6 个预约用户、12 段电价、初始逐槽 SOC [1.0, 0.9, 0.6, 0.4, 0.2]；预测/实际随机请求用独立 PCG64 流分离。输出 `candidate_network.json`、`mock_rl_data.json`、`dayahead_plan.json`、`mpc_run_result.json`。
- **旧数据**（暂未接入主流程）：
  - `data_generation_optim/`：真实高速沿线网络（13/8/6 入口）与城市人口数据（注意 `population.py` 里硬编码了高德地图 API key）；
  - `data_generation_rl/`：基于蔚来真实数据的 3 站 6 入口 70 天×24h 逐时泊松需求、分时电价。

---

## 三、当前状态与进度

### 最近 git 提交（研究方向一目了然）

```
4f677ab 增加违约成本+初步运行结果
4f5eb35 做了一些初步的简化
b9c8584 gx
862a2d7 数学模型大致修改完成
b6daaa1 更新MPC formulation
bea8c09 数学模型前的修改
51ec111 改成在原有启发式分配的基础上动态调整分配方案了
c02115c main_10论文在目标函数中决策超过预测域的部分，用边际库存价值进行了修正
f154f02 增加注释-mpc模型
d27cb22 初版mpc模型
...
```

工作区有 52 个未提交改动文件，**全部在 `ExpresswayBSSs/` 下**，CleanRL 本体完全未动。

### 已完成的里程碑

- **8/30–8/31：论文路径-事件 MILP 接入完成**。滚动求解从 `EVENT_PATH_ENUM_REPLAY`（枚举回放）升级为 `PAPER_GUROBI_OPTIMAL`（联合 Gurobi MILP），默认六站 12 轮全部 OPTIMAL（Gurobi 12.0.1 本机许可）。
- 最新 seed=42 六站运行结果：预约服务 14 次、随机 17 次、净收益 505.387、调整成本 13、违约 0、13 次路径发布。
- 已产出：`mpc_run_statistics.{json,md,csv}`、`paper_mpc_gurobi_validation.{md,json}`、`cost_stress_test_results.{md,json}`（等待/违约压力测试）。
- 论文 `paper/main.tex`（1007 行）为当前版；`mian11_fixed.tex` 为旧版；部分插图仍是占位符。

### 尚未做 / 下一步（按 todo.md / exp.md）

1. 协议固化：多种子配对比较；
2. 基线与消融：预测域 H 扫描、β=0 消融、规则充电基线 M1/M2；
3. 完美信息全日 MILP 上界；
4. **接入真实 PPO**（最大空缺）：
   - 需新建 `src/env_bss.py`（Gymnasium 环境：观测=逐槽 SOC+电价窗口+预约聚合，动作=站×槽连续充电功率 Box，奖励=已实现收益−充电成本−调整成本）；
   - 重写 `RL/ppo.py` 为连续动作 squashed-Gaussian PPO + DeepSets 置换不变 critic；
   - 目前 MPC 中的 RL 信号全部由 `MockRLProvider`（"低 SOC 槽优先"充电规则 + 线性假 critic）提供，**结果中标注 `signal_source="mock"`，不能当作 RL 性能解读**。

---

## 四、给"快速上手"的三个提醒

1. **别看 `cleanrl/` 找研究代码**——那是原版库，你的东西全在 `ExpresswayBSSs/`。
2. **`RL/ppo.py` 和 `main.py` 是未接入的草稿**，v3 计划明确本轮不动它们；当前实验是纯 MPC。
3. 读代码的顺序建议：`code_revision_plan_v3.md` → `run_mpc.py` → `src/continuous_runner.py` → `src/paper_mpc.py` → `src/event_engine.py` → `data_generation_test/parameter.py`。
