# 后续代码修改记录

记录日期：2026-09-09  
适用项目：`ExpresswayBSSs_v2`  
状态：待后续修改代码。

## 用户进入高速时发布路径

用户实际进入高速公路时，立即向其发布最近一次优化后保存的换电计划，后续实际行驶按该计划执行。

- 不必等到下一次路径更新时刻，也不要求用户进入后先沿用日前路径。
- 两类预约用户的路径仍每 15 分钟重新优化一次。进入高速时只发布已有计划，不额外触发一次路径优化。
- 尚未得到新的优化结果时，保留的初始计划为日前路径。
- 进入高速前，优化后的计划不发布；目标函数中相对日前路径的惩罚不计入实际费用或强化学习回报。
- 实际调整费用仍按实际发布且路径发生改变时计入一次，不能重复结算进入前各轮优化中的惩罚。发布后的路径作为后续调整的比较依据。

此项进入高速时的发布规则只在后续代码中实现，不加入论文。


## 固定运营期末的结算口径（暂不处理）

记录日期：2026-09-12。用户决定：暂时保留当前处理方式，不修改相关代码，后续再统一论文与代码的期末结算口径。

当前 `ExpresswayBSSs_v2` 默认运行 144 段、每段 5 分钟，共 12 小时。临近结束时预测域随剩余运营时间缩短；最后一次换电决策在运营计时 11 小时 55 分发生，执行最后一段后停止，不再执行 12 小时边界上的换电，也不继续处理剩余用户。

- 最后一段结束时，未服务且截止严格早于 12 小时的预约判为失败，计一次罚款并取消后续服务。
- 截止恰好等于或晚于 12 小时的请求继续保留在最终等待状态；未完成行程的用户保留为 active，不强行判失败。
- 最终收益只累计已经发生的账本事件，不包含这些剩余用户在 12 小时之后的服务收入、充电成本或失败罚款；结果保留 active_reservations、pending_requests 等统计。

这不必然是建模错误：若评价目标是固定 12 小时内的实际运营表现，该处理可以成立，但必须同时报告期末未完成任务，不能将其解释为这批用户全部处理后的完整收益。固定结束时间也会影响末期决策，因为结束后的收入和成本不在优化目标中。

论文第 5 节目前规定需求期结束后继续处理已有预约和等待请求，直至完成或失败，与上述固定时间截断口径不同。该差异与是否添加终端价值项是两个独立问题。

后续可选择：保留固定时间评价并调整论文与指标说明；或区分需求停止时刻和运营结算结束时刻，停止产生新需求后继续处理已有用户。当前尚未决定采用哪种修改方案，暂不实施，也不通过期末统一判失败代替后续结算。
