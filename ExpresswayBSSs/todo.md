# MPC-RL 后续工作清单（2026-09-02 瘦身版）

> 本文件只保留未完成工作的勾选状态，不再重复设计细节：
>
> - 数值实验设计（协议、多种子、消融、上界、指标与图表）以 `exp.md` 为准；
> - 代码架构设计以 `code_revision_plan_v3.md` 为准；
> - 旧版 MPC/RL 实现计划已归档至 `docs/archive/`，仅作历史记录。

## 已完成

- [x] 预约不可行惩罚：按预约换电事件计可配置违约成本；预约缺供时先用尽物理
  满电库存，随机用户不得越级；区分预测风险与实际违约。（v3 压力验收：
  0 kW 场景产生一次 1000 的预约失败成本且下游失活）

## 待办

- [ ] **确认"调整成本为 0"问题已解决**：v3 压力验收中已观察到非零改路成本，
  但尚未在标准默认算例上确认原始问题（增大规模/减小成本系数/放宽基准路径
  生成）不再出现。
- [ ] **统一、可复现的实验协议**（exp.md P0）：相同输入样本、配对比较、
  参数配置化、完整运行元数据、统一结果口径。
- [ ] **多随机种子稳健性实验**（exp.md E1）：≥30 个种子，训练/验证/测试
  种子分离，配对差值置信区间。
- [ ] **消融与可解释基线**（exp.md E2）：`H=1`、`beta=0`、静态日前、
  规则充电基线及组合消融。
- [ ] **完美信息全日 MILP 上界**（exp.md UB）：非因果上界，用于分解
  预测误差、有限时域与策略造成的最优性损失。
- [ ] **RL（PPO）实现**：目前没有任何现行文档覆盖——`plan_rl.md` 已归档
  且基于旧 MPC 接口，v3 计划明确排除 RL 实现。实施前需按 v3 的连续事件
  接口（`ContinuousEventEngine` / `paper_mpc.py`）重写实现计划。
- [ ] **接入并评估训练后的 PPO-MPC**（exp.md E5）：真实 actor/critic 替换
  Mock 信号，与规则基线、消融配置和完美信息上界在相同测试种子上比较。
