__pycache__/
*.py[cod]
# Track all output files, including those ignored by parent rules.
!outputs/
!outputs/**
# Exclude runtime lock files (e.g. controller/worker locks with restricted ACLs).
*.lock
